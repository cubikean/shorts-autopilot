requests>=2.31
python-dotenv>=1.0
yt-dlp>=2024.1.1
faster-whisper>=1.0.0
# LLM providers (LLM_PROVIDER=claude needs none: it calls the Claude Code CLI).
openai>=1.0.0
google-genai>=1.0.0
# OpenCV 5 dropped cv2.CascadeClassifier, which the face-tracking fallback uses.
opencv-python>=4.8.0,<5
# NVIDIA GPU transcription (no torch needed): CUDA runtime libraries for
# faster-whisper. Harmless without a GPU — Whisper falls back to CPU.
nvidia-cublas-cu12; platform_system != "Darwin"
nvidia-cudnn-cu12==9.*; platform_system != "Darwin"
# Publishing to YouTube (feed.py publish).
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
